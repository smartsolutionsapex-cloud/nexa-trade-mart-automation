import React from 'react';\nimport { FaWhatsapp, FaShoppingCart } from 'react-icons/fa';\n\nconst ProductCard = ({ product, onAddToCart, onWhatsAppOrder }) => {\n  return (\n    <div className=\"bg-white rounded-lg shadow-md hover:shadow-lg transition overflow-hidden\">\n      {/* Product Image */}\n      <div className=\"relative bg-gray-200 h-48 flex items-center justify-center\">\n        {product.image_url ? (\n          <img\n            src={product.image_url}\n            alt={product.name}\n            className=\"w-full h-full object-cover\"\n          />\n        ) : (\n          <div className=\"text-gray-400 text-4xl\">📦</div>\n        )}\n        <span className=\"absolute top-2 right-2 bg-red-500 text-white px-3 py-1 rounded-full text-sm font-bold\">\n          {Math.round(((product.selling_price - product.real_cost) / product.selling_price) * 100)}% PROFIT\n        </span>\n      </div>\n\n      {/* Product Info */}\n      <div className=\"p-4\">\n        <h3 className=\"text-lg font-bold text-gray-800 truncate\">{product.name}</h3>\n        <p className=\"text-sm text-gray-500 capitalize mb-2\">{product.category}</p>\n\n        {/* Stock Status */}\n        <p className={`text-sm font-semibold mb-3 ${\n          product.stock_quantity > 0 ? 'text-green-600' : 'text-red-600'\n        }`}>\n          {product.stock_quantity > 0 ? `✅ ${product.stock_quantity} in stock` : '❌ Out of stock'}\n        </p>\n\n        {/* Pricing */}\n        <div className=\"mb-4\">\n          <p className=\"text-2xl font-bold text-blue-600\">R{product.selling_price}</p>\n          <p className=\"text-sm text-gray-500 line-through\">R{product.real_cost}</p>\n        </div>\n\n        {/* Action Buttons */}\n        <div className=\"flex gap-2\">\n          <button\n            onClick={() => onAddToCart(product)}\n            disabled={product.stock_quantity === 0}\n            className=\"flex-1 bg-blue-600 text-white py-2 rounded-lg font-semibold hover:bg-blue-700 disabled:bg-gray-400 transition flex items-center justify-center gap-2\"\n          >\n            <FaShoppingCart /> Add\n          </button>\n          <button\n            onClick={() => onWhatsAppOrder(product)}\n            className=\"flex-1 bg-green-500 text-white py-2 rounded-lg font-semibold hover:bg-green-600 transition flex items-center justify-center gap-2\"\n          >\n            <FaWhatsapp /> Order\n          </button>\n        </div>\n      </div>\n    </div>\n  );\n};\n\nexport default ProductCard;\n